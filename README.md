

# Auto ID3Tag

Patch ID3 tags from file's name and directories and/or rename MP3 files from ID3 tags.

It relies on the 'id3v2' command, so it's probably Linux-only, and you need to have it installed and in your $PATH.

